# SSH
0. a Bash script that uses ssh to connect to your server using the private key ~/.ssh/school with the user ubuntu.<br />
1. Bash script that creates an RSA key pair.<br />
2. Client configuration file<br />
3. Add another SSH public key to the server.